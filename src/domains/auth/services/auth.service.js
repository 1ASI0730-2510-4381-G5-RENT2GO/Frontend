// src/domains/auth/services/auth.service.js

export function login(email, password) {
    // Aquí va la lógica de tu login o puede ser un mock de prueba
    return Promise.resolve({ success: true });
}